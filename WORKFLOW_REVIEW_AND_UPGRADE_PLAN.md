# n8n 保险内容平台：现有工作流评估与可落地升级方案

> 面向对象：保险经纪（使用者）→ 美国市场潜在/现有客户（阅读者）
> 目标：从“能跑”升级为“稳定可运营、可风控、可扩展”的内容生产平台。

---

## 一、整体架构设计

### 1) 核心模块划分（建议四层）

1. **内容生产层（n8n Workflows）**
   - Workflow A：每日晨报（新闻抓取→筛选→翻译→重写→入库）
   - Workflow B：素材库（日更选题→AI生成→质检→入库）
   - Workflow Q：质量与风控（事实检查、合规检查、敏感表达过滤）
   - Workflow D：分发与通知（7:30发布通知、失败告警、人工审核提醒）

2. **内容存储层（建议 Supabase / PostgreSQL）**
   - `news_items`（原始新闻与提取字段）
   - `content_items`（可展示内容，晨报+素材统一）
   - `review_queue`（待审核）
   - `dispatch_logs`（推送日志）
   - `workflow_runs`（执行审计）

3. **内容展示层（Web）**
   - 首页（今日晨报 + 推荐素材）
   - 历史归档（按日期）
   - 素材库（按分类/标签/场景）
   - 内容详情页（图文、来源、风险提示、可转发文案）

4. **定时分发层（Scheduler + Multi-channel）**
   - 固定时间触发 + 重试
   - 邮件、企业微信/钉钉/Telegram 等渠道

### 2) 模块 A 与 B 的关系

- **生产侧独立**：A 关注“时效新闻”，B 关注“长期销售素材”。
- **存储侧统一**：统一写入 `content_items`，通过 `content_source` 区分 `morning_brief` / `library`。
- **展示侧统一分区**：一个网页系统、两个主分区。

### 3) MVP 与后续扩展

- **MVP（2~3周）**
  - 跑通 A（抓取→Top10→翻译重写→入库→网页展示）
  - 简化 B（每日 2 篇固定模板）
  - 基础风控（来源白名单+敏感词过滤+人工审核开关）
- **扩展（4~8周）**
  - 多模型交叉校验、自动事实核查
  - 个性化分发（按经纪人偏好）
  - 多语言版本（中英双语）

---

## 二、模块 A（每日晨报）n8n 工作流设计

## A-1 建议工作流

`Cron(07:00 ET)` → `Source Fetch` → `Normalize` → `Quality Score` → `Dedup` → `Top10` → `AI Translate` → `AI Rewrite` → `Compliance Check` → `Image Plan` → `Save DB` → `Generate Daily Page` → `07:30 Push`

### 1) 定时触发

- **现状问题**：你当前触发为 7:00，但业务要求 7:30。
- **改进**：
  - 7:00 执行采集与生成
  - 7:25 执行质量检查与页面构建
  - 7:30 执行发布与推送
- **n8n建议**：拆成 3 个 Workflow，避免单流程超时。

### 2) Google 热点/相关新闻获取

- 组合来源（至少 4 类）：
  - Google News RSS（已有）
  - 保险媒体 RSS（Insurance Journal、NAIC 发布页、carrier newsroom）
  - 政策监管源（state DOI、IRS retirement updates）
  - 可选 GDELT / NewsAPI（作为补充）
- 增加“来源权重字段”`source_weight`，高可信源优先入选。

### 3) Top10 筛选逻辑（建议分层）

总分 = 关键词分 + 来源可信分 + 时效分 + 客户相关性分 - 风险分

- 关键词分：life/annuity/retirement/family protection
- 来源可信分：白名单媒体 +10，未知源 -5
- 时效分：24h 内 +8，48h 内 +4
- 客户相关性：是否能解释为家庭保障/退休规划 +10
- 风险分：纯财经投机、无保险关联、标题党等 -10~-30

### 4) 去重逻辑

- **现状问题**：仅按标题归一化去重，容易漏掉“同事件不同标题”。
- **改进**：
  1. URL 规范化去重（去 UTM）
  2. 标题 MinHash/SimHash 去重（相似度 >0.88）
  3. 同主题聚类（实体+时间窗口）

### 5) AI 摘要 + 翻译 + 重写

建议拆成三步，便于质检：

1. `Summarize EN`（事实抽取，列 3~5 条）
2. `Translate ZH`（术语表约束：beneficiary=受益人、cash value=现金价值）
3. `Rewrite for Client`（经纪人转发语气，含“为什么与你有关”）

### 6) 图文策略（A）

- 新闻解读：优先“场景插画/信息图”，避免人物肖像风险。
- 配图来源优先级：
  1. 自建版权图库
  2. 可商用图库（Pexels/Unsplash API，标注来源）
  3. AI 生成图（仅当无可用图库）
- 图片字段：`image_source`, `license_type`, `author`, `attribution_required`。

### 7) 页面生成与归档

- **现状问题**：Webhook 动态拼 HTML，缺少发布版本快照。
- **改进**：
  - 每日生成 `daily_snapshot`（JSON + HTML）
  - 详情页从 DB 查询，不依赖数组下标
  - 历史归档按 `publish_date` + `slug` 可回溯

### 8) 7:30 更新与推送

- 7:30 固定推送摘要（邮件/IM）
- 推送内容：今日 10 条标题 + 3 条“建议转发优先级”
- 失败自动重试：指数退避（1m/5m/15m）

### 9) 日志、异常与告警

- 每个流程写入 `workflow_runs`：开始时间、结束时间、成功率、失败节点。
- 失败告警：Slack/Telegram + 邮件双通道。
- 关键SLA：7:30 前必须有可展示内容；不足 10 条时触发“降级策略”。

---

## 三、模块 B（销售素材库）n8n 工作流设计

### 1) 日更生成机制

`Cron(08:00 ET)` → `Topic Planner` → `Persona Mapper` → `AI Draft` → `Compliance Gate` → `Image Select` → `Save DB`

- 每日建议 3~5 篇：
  - 新闻解读 1
  - 科普 1
  - 案例 1
  - 产品介绍/沟通话术 1~2

### 2) 新闻类 vs 非新闻类区分

- `content_mode = news_interpretation | evergreen`
- 新闻类必须绑定 `source_url`。
- 非新闻类必须绑定 `scenario`（如：首次破冰、家庭保单盘点、退休焦虑沟通）。

### 3) 面向“经纪人转发客户”的图文内容

每篇固定结构：

1. 开场破冰（80~120字）
2. 核心观点（3点）
3. 客户场景案例（1个）
4. 可转发短文案（30~60字）
5. 风险提示（非投资建议/条款以合同为准）

### 4) 素材库分类建议

- 一级：`life_insurance`, `annuity`
- 二级：`education`, `case`, `product_intro`, `objection_handling`, `client_followup`
- 标签：`新客破冰`, `家庭保障`, `退休现金流`, `税务延递`, `高净值`, `中产家庭`

### 5) 科普与转化平衡

- 增加 `sales_intent_score`（0~100）
  - <40：纯科普
  - 40~70：轻转化
  - >70：强引导（仅内部可见，默认不对客户直接显示）

---

## 四、网页平台设计建议

### 首页结构

1. 顶部：今日更新状态 + 最后更新时间
2. 区块A：今日晨报（Top10）
3. 区块B：推荐素材（按场景）
4. 区块C：历史归档入口
5. 区块D：快速操作（复制转发文案、收藏）

### 必备功能

- 分类、标签、日期筛选
- 卡片图文展示（封面 + 摘要 + 场景标签）
- 详情页含：正文、来源、适用客户、建议转发话术
- 搜索（标题、标签、全文）
- 收藏、复制“微信/短信版”转发文案

---

## 五、图文内容策略

### 1) 图片来源策略

- 优先商用可追溯来源，AI 图仅补位。
- 所有图片写入授权信息字段，避免版权争议。

### 2) 图文搭配建议

- 新闻政策类：信息图/图标化卡片
- 家庭案例类：温和生活场景图
- 产品解释类：流程图/对比图（term vs whole life）

### 3) 避免“纯文字”

- 正文每 250~300 字插入一个视觉模块（图/要点框/时间轴）。
- 卡片摘要控制在 2~3 行。

---

## 六、风控与校验机制

### 1) 新闻源白名单

- 仅允许白名单源进入自动发布；非白名单进入待审核。

### 2) 低质量与重复过滤

- 标题党、广告软文、无来源文章拦截。
- 24小时内相似内容仅保留 1 篇主文 + “延伸阅读”。

### 3) AI 幻觉控制

- 强制“引用事实字段”输出：`fact_1..fact_5` + `source_url`。
- 二次模型审校：检查是否出现“原文未提及”的结论。

### 4) 翻译质量校验

- 术语表 + 禁译词表。
- 回译抽检（ZH→EN）比较语义偏差。

### 5) 客户转发适配拦截

- 屏蔽恐慌营销、保证收益、夸大理赔等表达。
- 对“产品收益承诺”自动打回人工审核。

### 6) 人工审核开关

- `auto_publish = false`（默认）
- 经纪团队成熟后可按分类开启自动发布（如科普类自动，产品类人工）

---

## 七、数据存储与内容管理建议

## 建议字段（核心）

- 基础：`id`, `slug`, `title_zh`, `summary_zh`, `content_html`, `language`
- 业务：`content_source`, `category_l1`, `category_l2`, `tags`, `scenario`
- 来源：`source_url`, `source_name`, `source_publish_at`
- 风控：`risk_score`, `review_status`, `review_reason`, `compliance_flags`
- 图像：`image_url`, `image_source`, `license_type`, `attribution`
- 运营：`publish_at`, `author_type(ai/human)`, `distribution_channels`

### 存储选型建议

- **MVP**：Airtable/Google Sheets（快）
- **生产**：Supabase/PostgreSQL（可扩展、支持检索与权限）
- **不建议长期仅用 Sheets**：并发、检索、关系字段能力不足。

---

## 八、MVP 实施方案（最小可落地）

### 第1阶段（1周）

- 固化 Workflow A 闭环
- 调整到 7:30 发布
- 增加失败告警与重试

### 第2阶段（1周）

- 增加网页归档与详情页稳定 ID
- 增加人工审核开关

### 第3阶段（1周）

- 上线简版 Workflow B（每日2篇）
- 首页整合晨报+素材

---

## 九、后续扩展建议

1. 多语言（中文主库 + 英文镜像）
2. 受众细分（新婚家庭/有孩家庭/临退人群）
3. 经纪人风格模板（稳健型、故事型、数据型）
4. 多渠道分发（邮件、短信、社媒、聊天工具）
5. 内容效果回流（点击、转发、咨询）驱动选题优化

---

## 十、你当前仓库工作流的关键问题与“正确改进意见”

> 以下是对当前 JSON 的直评（这部分是 Claude Code 容易扣分的点，建议优先改）。

1. **触发时间不满足业务目标**：A 是 7:00，不是 7:30。应拆分生产与发布时间，保证准时且可回滚。
2. **数据链路断裂风险**：A 中 `AI Translate Rewrite` 后未保留原始新闻字段，`Parse` 节点用 `raw.title/link` 可能为空，导致来源丢失。应在进入 LLM 前做字段快照并合并回写。
3. **去重策略过弱**：仅标题归一化，无法处理“同源改写标题”。应加入 URL 规范化 + 相似度去重。
4. **无事实校验与幻觉防护**：直接生成并入库，缺“事实抽取-校验-重写”三段式，风险高。
5. **图片合规不足**：仅用 Pollinations URL，未记录版权与授权信息。生产环境需要可追溯来源。
6. **Web 详情页索引不稳定**：`page=article&id=数组下标` 在筛选/排序变化后会错位。应改为 `slug` 或 UUID。
7. **XSS 风险**：直接拼接 `Chinese Content HTML` 到页面，未做白名单清洗。应在入库前 sanitize。
8. **状态管理不完整**：有 `pending_review` 字段但无审核流与发布流节点，建议新增 Review Queue Workflow。
9. **可观测性不足**：没有集中运行日志与失败指标，无法评估每日产出质量。
10. **存储扩展性不足**：全量读取 Sheets 并拼 HTML，数据增长后会变慢。应加缓存或迁移数据库。

### 推荐优先级（按落地收益）

- P0：7:30 发布链路、错误重试、来源字段完整、详情页稳定 ID
- P1：风控校验、去重增强、人工审核开关
- P2：数据库迁移、个性化分发、效果回流

