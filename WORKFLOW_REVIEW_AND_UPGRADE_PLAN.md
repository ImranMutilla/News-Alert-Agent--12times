# n8n 保险内容平台：基于三层架构的无人工干预升级方案（修订版）

> 目标：保留你现有的“三层工作流”主架构（生产层、存储层、展示层），在不引入人工审核的前提下，补齐稳定性、质量、风控与可扩展性。

---

## 一、整体架构设计（按你认可的三层）

你现有三层方向是对的，建议保持：

1. **内容生产层（n8n Workflow A + B）**
   - A：每日晨报（新闻抓取→筛选→翻译→改写→图文生成→入库）
   - B：素材库（日更选题→生成→图文生成→入库）
   - **质量与风控不再单独拆层**，直接内嵌在 A/B 的模型链路里完成。

2. **内容存储层（Google Sheets 可继续，后续可迁移）**
   - `newsletter`
   - `content_library`
   - 增加日志表（可选）`workflow_logs`

3. **内容展示层（Workflow C Webhook）**
   - 首页：今日晨报 + 素材库
   - 历史归档：按日期
   - 详情页：图文内容

### MVP 与扩展

- **MVP（保持轻量）**：继续 Sheets + 3 个 Workflow，不加人工审核流程。
- **扩展**：将质量校验做成 A/B 内的“模型自检 + 规则校验”节点，不新增人工环节。

---

## 二、模块 A（每日晨报）可执行改进（无人工审核）

## A-1 你当前流程的核心问题（针对现有 JSON）

1. 调度是 7:00，未对齐“7:30 准时更新”。
2. `AI Translate Rewrite` 后原始字段有丢失风险（title/link/pubDate 可能在后续解析不可用）。
3. 去重仅标题归一化，无法去掉同事件改写稿。
4. 质量与风控全靠单次生成，无模型自检，幻觉/夸大风险偏高。
5. 图片只用 AI URL，缺少来源质量策略与兜底。
6. Web 详情用数组 index，不稳定。

## A-2 推荐的节点级增强（仍然是一个 A 工作流）

`Cron 07:00` → `Fetch RSS` → `Normalize` → `Dedup+Score` → `Top10` → `LLM-Step1(事实摘要)` → `LLM-Step2(专业翻译)` → `LLM-Step3(客户化改写)` → `LLM-Step4(自检风控)` → `Image Strategy` → `Save` → `07:30 发布通知`

### 1) 时间策略（不增加人工）

- 07:00 启动内容生产
- 07:25 完成入库与页面可用性检查
- 07:30 自动推送
- 若不足 10 条：自动降级为“5 条精选 + 1 条科普补位”（来自素材库）

### 2) 去重策略补强

在 `Dedup Score Filter` 的 code 节点增加三重去重：
- URL 规范化去重（移除 UTM 参数）
- 标题相似度去重（Jaccard/SimHash）
- 同源同日同主题去重

### 3) 质量与风控“内嵌模型完成”（符合你的要求）

在 A 内新增 `LLM-Step4(自检风控)`，输出字段：
- `factual_consistency_score`（与原文一致性）
- `translation_quality_score`（术语准确性）
- `compliance_score`（是否含夸大承诺/误导）
- `client_readability_score`（客户可读性）
- `risk_flags[]`（如 guaranteed_return / fear_marketing）

自动规则：
- 若 `compliance_score < 0.75` 或出现高风险 flags：**自动重写一次**（走二次提示词）
- 若二次仍不达标：**自动降级发布为“简讯版”**（仅事实+风险提示，不给销售引导）

> 以上全自动，不人工介入。

### 4) 英文→中文翻译质量

- 建立术语表（固定映射）注入 prompt：
  - beneficiary=受益人
  - cash value=现金价值
  - term life=定期寿险
  - whole life=终身寿险
  - annuity rider=年金附加条款
- 翻译后做“回译抽检”（模型自动），偏差过大自动重译。

### 5) 图文生成策略

- 优先图像类型：信息图感、家庭场景、退休规划情境。
- 图片失败兜底：
  1) 首选 AI 图 URL
  2) 失败则 fallback 到预置版权安全占位图
- 在表中新增：`image_style`, `image_fallback_used`。

### 6) 7:30 推送可靠性

- 推送节点失败重试：1m / 3m / 10m
- 超过重试次数：自动发告警到运维频道（不是人工审核，只是技术告警）

---

## 三、模块 B（素材库）可执行改进（无人工审核）

## B-1 目标

继续每日自动产出，且面向“经纪转发给客户”可直接使用。

## B-2 建议流程

`Cron 08:00` → `Topic Planner` → `Persona/Scenario Mapper` → `LLM Draft` → `LLM Self-QC` → `Image Plan` → `Save to content_library`

### 1) 选题结构（每天 4 篇）

- 新闻解读 1
- 科普教育 1
- 生活案例 1
- 产品理解/沟通话术 1

### 2) 内容结构模板（固定）

每篇强制输出：
1. 破冰开场
2. 3 个关键点
3. 生活化案例
4. 可直接转发短文案
5. 温和 CTA（不硬广）

### 3) 风控内嵌

`LLM Self-QC` 自动检查：
- 是否恐慌营销
- 是否收益保证
- 是否不当承诺
- 是否不适配美国客户语境

不达标自动重写，最多两轮，然后自动降级为中性教育稿。

---

## 四、网页平台（C）改进重点

你现有 C 可以继续，但建议补 4 点：

1. **详情页 ID 改为稳定键**
   - 不用数组 index，改为 `slug`（如 `2026-04-01-life-insurance-001`）
2. **缓存与分页**
   - 避免全量读取 Sheets 后一次性渲染
3. **安全清洗**
   - 对 `Chinese Content HTML` 做白名单标签过滤（防 XSS）
4. **更适合转发的交互**
   - 增加“一键复制转发文案”按钮

---

## 五、图文内容策略（全自动）

### 图片来源策略
- 主策略：AI 图生成（统一风格）
- 兜底策略：静态版权安全图库
- 字段记录：`image_provider`, `image_prompt`, `image_fallback_used`

### 图文排版策略
- 每 250~300 字一个视觉分段
- 卡片摘要 2~3 行
- 标题避免行业黑话，强调“和家庭保障有什么关系”

---

## 六、风控与校验机制（无人工干预版）

重点：**自动化风控，不人工审核**

1. 新闻源白名单：白名单高权重，非白名单低权重但不直接丢弃。
2. 低质过滤：标题党、广告软文自动剔除。
3. 幻觉控制：先抽取事实点再改写，不允许越界信息。
4. 翻译质检：术语表 + 回译一致性。
5. 客户适配：过滤极端措辞、夸大承诺、监管敏感表达。
6. 自动降级：高风险内容转为中性简讯，不阻断全流程。

---

## 七、数据存储建议（兼容你当前方案）

你可以继续用 Sheets，但建议至少补这些字段：

- `slug`
- `source_name`
- `source_weight`
- `factual_consistency_score`
- `translation_quality_score`
- `compliance_score`
- `risk_flags`
- `image_fallback_used`
- `delivery_status`
- `delivery_retry_count`

这样无需改三层架构，也能显著提升可运营性。

---

## 八、MVP 最小改造清单（建议按周）

### Week 1（必须做）
- A 调整为 7:30 准时发布链路
- A/B 内嵌 LLM 自检节点
- 三重去重 + 自动重写/降级

### Week 2（强烈建议）
- C 详情页改 `slug`
- HTML 白名单清洗
- 推送重试与失败告警

### Week 3（可选）
- Sheets 增补质量字段
- 首页新增“可复制转发文案”

---

## 九、结论（直接回答你的意见）

你说得对：
- **四层拆法不如三层高效**（在你当前阶段）。
- **质量与风控应在晨报/素材库模型链路直接完成**，不必另起“人工审核流程”。

所以最优解不是推翻你原设计，而是：
- 保持三层
- 把质控风控内嵌到 A/B 的 LLM 多步链路
- 用自动重写、自动降级、自动重试替代人工介入

这套方案更符合你要的“部署后全自动、低干预、高效率”。
